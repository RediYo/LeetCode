import ExtraNode.ListNode;

public class Code23 {

    public ListNode mergeKLists(ListNode[] lists) {

        //最初的想法是针对所有的链表数组对于首元素找到一个最小值，
        //然后进行是最小值的那个链表进行元素后移得到首元素，
        //接着添加这个首元素到元素排序队列中，可以使用优先级队列得到每个最小值，
        //同时使用哈希表保存首元素与链表的对应关系
        return null;
    }
}
